/* eslint-disable */
// app/(tabs)/profile.tsx
// Profile tab screen that renders the ProfileForm

import ProfileForm from '../(auth)/profile-form';

export default function ProfileScreen() {
    return <ProfileForm />;
}
